// Global test setup — runs before every test file.
// Defines the __DEV__ global that React Native provides at runtime.
// Many service modules guard console.log/error calls behind __DEV__.
global.__DEV__ = true;
